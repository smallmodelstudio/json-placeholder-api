pipeline:
  name: Contract tests
  identifier: json_placeholder_api_contract_tests
  projectIdentifier: json_placeholder_api
  orgIdentifier: default
  tags: {}
  properties:
    ci:
      codebase:
        connectorRef: account.github_connector
        build: <+input>
  stages:
    - stage:
        name: Contract tests
        identifier: contract_tests
        type: CI
        spec:
          cloneCodebase: true
          platform:
            os: Linux
            arch: Amd64
          # Local runner, same as ci.yaml — see docs/README-harness.md.
          runtime:
            type: Docker
            spec: {}
          execution:
            steps:
              - step:
                  type: Run
                  name: Install dependencies
                  identifier: install_deps
                  spec:
                    shell: Sh
                    command: npm ci
              - step:
                  type: Run
                  name: Contract tests against real upstream
                  identifier: contract_tests
                  spec:
                    shell: Sh
                    # Hits the real jsonplaceholder.typicode.com — deliberately
                    # run on a schedule (see triggers/contract-tests-cron.yaml)
                    # rather than as a PR gate, so upstream flakiness never
                    # blocks a merge.
                    command: npm run test:contract
